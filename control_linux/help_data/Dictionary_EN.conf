# English dictionary - English is the GUI default, so no translations are needed here.
# NFmiSettings::Read() throws "Error parsing the end of the configuration file" for a file
# that contains nothing but comments, so at least one assignment must be present.

DictionaryLanguage = EN
